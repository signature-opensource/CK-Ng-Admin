<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- Res/** is auto-embedded by CK.EmbeddedResources.Abstractions (buildTransitive .props/.targets).
         signature-one-blanc.png is picked up with no explicit EmbeddedResource entry needed. -->

    <ItemGroup>
        <PackageReference Include="CK.AppIdentity.Abstractions" Version="0.14.1" />
        <PackageReference Include="CK.Mail.SharedLayout" Version="0.2.0--ci.10" />
    </ItemGroup>

</Project>
