<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="CK.IO.UserManagement.UserBanned" Version="0.2.1" />
        <!-- TODO: version of the CK-Ng-User-UserProfile release, the same one as the family pinned
             in the Sample. Brings the banned session support and, transitively, the actor channel
             it rides on. -->
        <PackageReference Include="CK.Ng.UserProfile.UserBanned" Version="0.8.1" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\CK.Ng.Admin.UserManagement\CK.Ng.Admin.UserManagement.csproj" />
    </ItemGroup>

</Project>
